// main.js

document.addEventListener('DOMContentLoaded', () => {
    const gameContainer = document.getElementById('game-container');
    gameContainer.textContent = 'ゲーム開始！';
    // 今後、ここにゲームロジックを追加していきます。
});
